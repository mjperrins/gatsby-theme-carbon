{
  "name": "example",
  "private": true,
  "version": "1.15.3",
  "license": "Apache 2.0",
  "scripts": {
    "develop": "gatsby develop",
    "develop:clean": "yarn clean && yarn develop",
    "build": "gatsby build",
    "serve": "gatsby serve",
    "clean": "gatsby clean",
    "build:prefix": "gatsby build --prefix-paths",
    "serve:prefix": "gatsby serve --prefix-paths",
    "test:prefix": "yarn build:prefix && yarn serve:prefix"
  },
  "dependencies": {
    "gatsby": "^3.0.0",
    "gatsby-theme-carbon": "^1.15.3",
    "react": "^16.8.6",
    "react-dom": "^16.8.6"
  }
}
